//
// Created by Katie He on 3/8/25.
//

#include <iostream>
#include <vector>
using namespace std;

/*
int main()
{
    int n;
    cin >> n;
    int count = 0;
    string s;
    cin >> s;
    vector<int> v;
    for(int i = 0; i < n / 2; ++i)
    {
        if(s[2 * i] == 'G' && s[2 * i + 1] == 'H')
            v.push_back(1);
        else if(s[2 * i] == 'H' && s[2 * i + 1] == 'G')
            v.push_back(0);
    }
    v.push_back(0);
    for(int i = 0; i < v.size(); ++i)
    {
        if(v[i] != v[i + 1])
            ++count;
    }
    cout << count;
}
 */