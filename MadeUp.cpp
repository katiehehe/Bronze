//
// Created by Katie He on 2/24/25.
//

#include <iostream>
#include <unordered_map>
#include <vector>
using namespace std;

/*
int main()
{
    int n;
    cin >> n;
    unordered_map<int, int> frequency;
    for(int i = 0; i < n; ++i)
    {
        int x;
        cin >> x;
        frequency[x] += 1;
    }
    vector<int> b(n);
    for(int i = 0; i < n; ++i)
    {
        cin >> b[i];
    }
    long long count = 0;
    for(int i = 0; i < n; ++i)
    {
        int index;
        cin >> index;
        index -= 1;
        count += (long long)(frequency[b[index]]);
    }
    cout << count;
}
 */