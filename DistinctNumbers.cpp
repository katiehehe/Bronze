//
// Created by Katie He on 1/26/25.
//
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

/*
int main()
{
    int n;
    cin >> n;
    cin.ignore();
    vector<int> nums(n);
    for(int i = 0; i < n; ++i)
    {
        cin >> nums[i];
    }
    sort(nums.begin(), nums.end());
    int dist = 1;
    for(int i = 0; i < n - 1; ++i)
    {
        if(nums[i] != nums[i+1])
            ++dist;
    }
    cout << dist;
    return 0;
}
 */